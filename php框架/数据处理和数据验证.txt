# 数据类型(格式)
需要可以定义数据格式如 
```
map[string]string
struct{
	id   int require
	data require
}
```

## 常用
* map里面某个元素可选
* map里面某个元素必须
* map里面某个数据在某个数据库的某个字段上面不存在(一致性)
* map里面某个数据在某个数据库的某个字段上面存在(存在xx,才能xx)
* 某个数据满足email的格式
* 某个数据是数字

## 不常用
